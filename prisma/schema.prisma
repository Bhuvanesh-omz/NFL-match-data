datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model NflGameData {
  id        String      @id @default(uuid())
  gameId    Int
  data      Json     // stores the whole JSON object
  createdAt DateTime @default(now())
}
